---
components:
  requestBodies:
    devisetokenauth:
      Session:
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/devisetokenauth.Session"

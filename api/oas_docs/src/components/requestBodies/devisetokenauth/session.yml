---
components:
  requestBodies:
    devisetokenauth.Session:
      content:
        application/json:
          schema:
            "$ref": "../../schemas/devisetokenauth/session.yml#/components/schemas/devisetokenauth.Session"

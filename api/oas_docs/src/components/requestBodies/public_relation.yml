---
components:
  requestBodies:
    PublicRelation:
      content:
        application/json:
          schema:
            allOf:
            - "$ref": "#/components/schemas/PublicRelation"
            - type: object
              properties:
                imgur_deletehash:
                  type: string
                  nullable: true

services:
  swagger-ui:
    image: swaggerapi/swagger-ui
    container_name: "nutfes-gm3-swagger-ui"
    ports:
      - "8004:8080"
    volumes:
      - ./openapi:/openapi
    environment:
      SWAGGER_JSON: /openapi/openapi.yml

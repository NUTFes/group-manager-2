

server {
  listen 443 ssl http2;
    
  server_name group-manager.nutfes.net;

  ssl_certificate /var/lib/https-portal/group-manager.nutfes.net/production/chained.crt;
  ssl_certificate_key /var/lib/https-portal/group-manager.nutfes.net/production/domain.key;

  ssl_session_cache shared:SSL:50m;
  ssl_ciphers ECDHE-RSA-AES256-GCM-SHA384:ECDHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-SHA384:ECDHE-RSA-AES128-SHA256:ECDHE-RSA-AES256-SHA:ECDHE-RSA-AES128-SHA:DHE-RSA-AES256-SHA:DHE-RSA-AES128-SHA;
  ssl_prefer_server_ciphers on;

  ssl_dhparam /var/lib/https-portal/dhparam.pem;

  # Send HSTS header if configured
  

  

  

  

  

  
  location / {
    root   /var/www/vhosts/group-manager.nutfes.net;
    index  index.html;
  }

  location /mypage {
    try_files $uri $uri/ /index.html;
  }

  location /MyPage {
    try_files $uri $uri/ /index.html;
  }

  location /user_detail {
    try_files $uri $uri/ /index.html;
  }

  location /regist_shop {
    try_files $uri $uri/ /index.html;
  }

  location /regist_group {
    try_files $uri $uri/ /index.html;
  }

  location /group {
    try_files $uri $uri/ /index.html;
  }

  location /regist_food_booths {
    try_files $uri $uri/ /index.html;
  }

  location /regist_purchase {
    try_files $uri $uri/ /index.html;
  }

  location /edit_user_info {
    try_files $uri $uri/ /index.html;
  }

  location /password_reset {
    try_files $uri $uri/ /index.html;
  }

  location /mobile_mypage {
    try_files $uri $uri/ /index.html;
  }

  location /mobile_MyPage {
    try_files $uri $uri/ /index.html;
  }

  location /mobile_user_detail {
    try_files $uri $uri/ /index.html;
  }

  location /mobile_regist_shop {
    try_files $uri $uri/ /index.html;
  }

  location /mobile_regist_group {
    try_files $uri $uri/ /index.html;
  }

  location /mobile_group {
    try_files $uri $uri/ /index.html;
  }

  location /mobile_regist_food_booths {
    try_files $uri $uri/ /index.html;
  }

  location /mobile_regist_purchase {
    try_files $uri $uri/ /index.html;
  }

  location /mobile_edit_user_info {
    try_files $uri $uri/ /index.html;
  }

  location /mobile_password_reset {
    try_files $uri $uri/ /index.html;
  }

  
    
}
